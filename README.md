# fractal_tree
This project generates a beautiful, colorful fractal tree using recursion and Python’s built-in turtle graphics module.  It creates patterns like this 👇 🌳🌿🌀 Branches split recursively, colors fade with depth, and the whole animation draws itself in real time.
